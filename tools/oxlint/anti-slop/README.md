# Vendored anti-slop rules

This directory is reserved for the source copied from `dmmulroy/anti-slop`.

The upstream project explicitly recommends vendoring its `src/` rules rather than consuming an npm package. The repository will enable the rules only after the complete reviewed source is copied here and validated against the pinned Oxlint/@oxlint/plugins versions.

Upstream:

- https://github.com/dmmulroy/anti-slop

Do not place generated dependencies or secrets here.
