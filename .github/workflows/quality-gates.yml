name: Quality Gates

on:
  push:
    branches:
      - main
      - chore/engineering-quality-gates
  pull_request:

permissions:
  contents: write

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Vendor anti-slop
        run: npm run vendor:anti-slop

      - name: Generate lockfile when missing
        run: npm install --package-lock-only --ignore-scripts

      - name: Persist generated lockfile
        if: github.event_name == 'push' && github.ref == 'refs/heads/chore/engineering-quality-gates' && hashFiles('package-lock.json') != ''
        run: |
          if git diff --quiet -- package-lock.json; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json
          git commit -m "chore: add npm lockfile"
          git push

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build
